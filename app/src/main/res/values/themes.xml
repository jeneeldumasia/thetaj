<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Newrestaurant" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!-- Set the status bar color to transparent -->
        <item name="android:statusBarColor">@android:color/transparent</item>

        <!-- Set the navigation bar color to transparent -->
        <item name="android:navigationBarColor">@android:color/system_accent3_500</item>
    </style>

    <style name="Theme.Newrestaurant" parent="Base.Theme.Newrestaurant" />
</resources>